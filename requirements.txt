# NOTE: google-genai (Gemini) is intentionally omitted from the hosted demo to
# keep the Streamlit Cloud build small and reliable (it pulls cryptography,
# google-auth, websockets, etc.). The default provider is Groq. To enable
# Gemini in a self-hosted install, add: google-genai>=1.0.0
# Build trigger: force Streamlit Cloud cache invalidation by touching this file.
groq>=0.11.0
gitpython>=3.1.40
pydantic>=2.5.0
typer>=0.12.0
rich>=13.7.0
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0
# Pinned: Streamlit Cloud reinstalls deps on every reboot and would otherwise
# pull the newest Streamlit, whose API changes can crash the app at runtime
# ("Error running app"). These are the versions verified working.
streamlit==1.42.0
# streamlit-lottie removed: frontend assets fail to load on Streamlit Cloud.
# Lottie now rendered via @lottiefiles/lottie-player web component inside
# st.components.v1.html (see autodock/web.py:_render_lottie).
# cache-bust: force Streamlit Cloud rebuild for d038052 stCode fix
